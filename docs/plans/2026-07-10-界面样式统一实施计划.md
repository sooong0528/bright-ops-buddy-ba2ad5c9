# 界面样式统一实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 提高系统字号、简化巡检导航、统一主要页面容器，并优化巡检方案编辑抽屉。

**Architecture:** 通过 Tailwind 字号令牌和少量公共样式统一基础视觉；导航与抽屉仅修改现有组件，不引入新组件库或业务状态。主要列表页复用统一筛选工具栏类名。

**Tech Stack:** React、TypeScript、Tailwind CSS、shadcn/Radix UI、Vitest。

---

### Task 1: 字号与公共页面样式

**Files:**
- Modify: `tailwind.config.ts`
- Modify: `src/index.css`
- Modify: `src/pages/*.tsx`
- Modify: `src/components/*.tsx`

- [ ] 将 `text-xs` 令牌覆盖为 14px、20px 行高。
- [ ] 将业务组件中 `text-[10px]`、`text-[11px]`、`text-[12px]`、`text-[13px]` 替换为 `text-sm`。
- [ ] 增加统一的 `filter-bar` 公共样式，并应用到主要列表页筛选区。
- [ ] 使用 `rg -n 'text-\\[(10|11|12|13)px\\]' src` 检查无小字号残留。

### Task 2: 巡检一级菜单

**Files:**
- Modify: `src/components/AppSidebar.tsx`
- Modify: `src/components/AppLayout.tsx`
- Modify: `src/test/configuration-refinement.test.ts`

- [ ] 增加导航检查，要求存在“巡检结果”“异常记录”两个一级入口。
- [ ] 删除巡检中心子菜单结构，将两个入口放入 `mainItems`。
- [ ] 将 `/inspection` 顶栏标题改为“巡检结果”。
- [ ] 运行 `npm test -- --run src/test/configuration-refinement.test.ts`，预期全部通过。

### Task 3: 巡检方案抽屉

**Files:**
- Modify: `src/pages/InspectionAdmin.tsx`

- [ ] 抽屉宽度改为 820px，内容区改为更紧凑的纵向布局。
- [ ] 移除基本信息区域的启停开关，在标题旁展示状态 Badge。
- [ ] 底部左侧增加启用状态开关，右侧保留取消和保存按钮。
- [ ] 调整巡检项表格列宽，避免 820px 抽屉内输入框溢出。
- [ ] 运行 `npm run build`，预期构建成功；浏览器检查巡检结果、异常记录和巡检方案抽屉。

### Task 4: 导航与通用统计卡片

**Files:**
- Create: `src/components/StatCard.tsx`
- Modify: `src/components/ui/sidebar.tsx`
- Modify: `src/pages/Dashboard.tsx`
- Modify: `src/pages/Inspection.tsx`
- Modify: `src/pages/AbnormalRecords.tsx`
- Modify: `src/pages/Analysis.tsx`
- Modify: `src/pages/Reports.tsx`
- Modify: `src/pages/Assets.tsx`
- Modify: `src/pages/Knowledge.tsx`
- Modify: `src/pages/Users.tsx`
- Modify: `src/pages/Audit.tsx`
- Modify: `src/lib/mockData.ts`

- [ ] 将一级菜单调整为 16px 文字、20px 图标和 44px 高度。
- [ ] 新增通用 `StatCard`，统一标题、数值、说明和图标规格。
- [ ] 将 8 个一级页面的顶部统计区替换为通用组件和已确认指标。
- [ ] 放大驾驶舱四个业务区块标题与图标，并更新异常记录文案。
- [ ] 将 CPU 图表替换为 7 天关注项与异常项两条趋势线。
- [ ] 运行完整测试和生产构建，并检查 1440px、1920px 页面布局。
